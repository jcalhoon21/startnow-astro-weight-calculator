console.log("the parameters.js file has loaded");

function getFullName(whatever, whocares){

    var whatever = argument1;

    var whocares = argument2;

    return  firstname + " " + lastname;
}

var first = "Jen";

var last = "Calhoon";

var result = getFullName(first, last);

// var result = getFullName("Jen", "Calhoon");

console.log(result);